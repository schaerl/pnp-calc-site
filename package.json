{
  "name": "pnp-calc-site",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "del dist",
    "compile": "copyfiles -u 1 -e **/*.ts src/* src/**/* dist && npm run compile:ts",
    "compile:ts": "browserify ./src/script/index.ts -p [ tsify ] -t [ babelify --global --presets [ @babel/preset-env ] ]  > dist/bundle.js",
    "start": "http-server ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-env": "^7.21.4",
    "@tsconfig/recommended": "^1.0.2",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "copyfiles": "^2.4.1",
    "del-cli": "^5.0.0",
    "http-server": "^14.1.1",
    "tsify": "^5.0.4",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "pnp-calc": "file:../pnp-calc"
  }
}
